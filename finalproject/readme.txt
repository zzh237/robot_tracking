# finalproject.py will run without any chanages to VM

# To run the code in the folder ./report_code the following commands must be run:

sudo apt-get install python-pip=1.5.6-5ubuntu2 -y
sudo apt-get install cython=0.21.1-1 -y
sudo apt-get install libhdf5-serial-dev=1.8.13+docs-15 hdf5-tools=1.8.13+docs-15 -y
pip install numpy==1.12.1
pip install sklearn==0.0
pip install keras==2.0.3
HDF5_DIR=/usr/lib/i386-linux-gnu/hdf5/serial pip install h5py==2.7.0
